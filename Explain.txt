what are promises in javascript?

promises are  are objects that represent the eventual completion(or failure) of an asynchronous operation and its resulting value
cleaner manageable way to handle asynchronous operation compared to callback